version: "3.1"
intents:
  - greet

responses:
  utter_greet:
    - custom:
        type: "whatsapp_template"
        template_name:


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
