version: "3.1"

intents:
  - greet

# responses:

actions:
  - action_stt_greet

slots:
  user_whatsapp:
    type: text
    influence_conversation: false


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
